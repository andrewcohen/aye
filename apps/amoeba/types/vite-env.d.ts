// Vite's ambient declarations: `import.meta.env`, and the module shapes for
// non-code imports like CSS. Referenced rather than added to `compilerOptions.
// types`, which would replace the default "every @types package" behaviour and
// silently drop the rest.
/// <reference types="vite/client" />
